on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

name: pkgdown

jobs:
  pkgdown:
    # Pinned to 22.04 (jammy) to match the jammy RSPM binaries below: on
    # ubuntu-latest (24.04/noble) the jammy magick binary links the older
    # libtiff.so.5, which noble does not ship, breaking build_reference()
    # with "libtiff.so.5: cannot open shared object file".
    runs-on: ubuntu-22.04

    strategy:
      fail-fast: false
    env:
      R_REMOTES_NO_ERRORS_FROM_WARNINGS: true
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      CFBD_API_KEY: ${{ secrets.CFBD_API_KEY }}
      RSPM: "https://packagemanager.rstudio.com/cran/__linux__/jammy/latest"

    steps:
      - uses: actions/checkout@v5

      - uses: r-lib/actions/setup-r@v2

      - uses: r-lib/actions/setup-pandoc@v2

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          cache-version: 1
          extra-packages: |
            any::pkgdown
            any::animation
            any::baseballr
            any::hoopR
            any::fastRhockey
            any::ggimage
            any::knitr
            any::oddsapiR
            any::pacman
            any::pak
            any::ragg
            any::remotes
            any::rsconnect
            any::sportyR
            any::tictoc
            any::wehoop
            any::zoo
            local::.
          needs: website

      - name: Install system dependencies
        run: |
          pak::pkg_install("sportsdataverse/cfbfastR")
        shell: Rscript {0}

      - name: Deploy package
        run: |
          git config --local user.name "$GITHUB_ACTOR"
          git config --local user.email "$GITHUB_ACTOR@users.noreply.github.com"
          Rscript -e 'pkgdown::deploy_to_branch(new_process = FALSE)'
